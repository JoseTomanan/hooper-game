namespace Hooper.Player;

/// <summary>
/// Which hand the ball is currently displayed in (M7b, issue #73).
/// Cosmetic-only — see HandSideResolver's class doc.
/// </summary>
public enum HandSide
{
    Left,
    Right,
}
